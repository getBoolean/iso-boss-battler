extends Control

# player node
# boss node

# boss_hpbar node
# player_hpbar node
# player_mpbar node

# Called when the node enters the scene tree for the first time.
func _ready():
    # get player node
        
    # get boss node
        
    # get boss_hpbar node
    # get player_hpbar node
    # get player_mpbar node
    
    pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
    #set boss_hpbarnode.Value = (bossnode.BOSS_CUR_HP) / bossnode.BOSS_MAX_HP)
    #set player_hpbarnode.Value = (playernode.PLAYER_CUR_HP / playernode.PLAYER_MAX_HP)
    #set player_mpbarnode.Value = (playernode.PLAYER_CUR_MP / playernode.PLAYER_MAX_MP)
    
